name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: sreimers/action-archlinux-mingw
      with:
        build_cmd: "uname -a; pacman -Ql | grep mingw"